global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'synthetic-uptime-monitor'
    static_configs:
      - targets: ['app:8000']

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

rule_files:
  - "/etc/prometheus/rules/*.yml"