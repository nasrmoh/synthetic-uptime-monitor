## Test Plan — `target_scanner`, `perform_check`, `record_check_result`

### `target_scanner`

- [x] **`test_target_scanner_adds_job_when_expected_gt_current`**
  - Setup: test DB has more enabled targets than mock `scheduler.get_jobs()` returns
  - Mock: `scheduler.get_jobs()` → fake list of Job-like objects (some or none)
  - Assert: `scheduler.add_job` was called with the correct `target_id`(s)

- [x] **`test_target_scanner_removes_job_when_current_gt_expected`**
  - Setup: test DB has fewer enabled targets than mock `scheduler.get_jobs()` returns (i.e. a job exists for a target that's now disabled/gone)
  - Mock: `scheduler.get_jobs()` → fake jobs including one with no matching enabled target
  - Assert: `scheduler.remove_job` was called with the correct job id

- [x] **`test_target_scanner_noop_when_expected_equals_current`**
  - Setup: test DB enabled targets exactly match mock `scheduler.get_jobs()`
  - Assert: neither `add_job` nor `remove_job` was called

- [x] **`test_target_scanner_ignores_non_check_target_jobs`**
  - Setup: mock `scheduler.get_jobs()` includes a job like `"target_scanner"` itself
  - Assert: neither `add_job` nor `remove_job` was called for it (job filtered out before comparison)

### `perform_check`

- [x] **`test_perform_check_raises_when_target_missing`**
  - Setup: test DB has no row for the given `target_id`
  - Assert: `TargetNotFoundError` is raised

- [x] **`test_perform_check_skips_when_target_disabled`**
  - Setup: test DB has a row with `enabled=False`
  - Mock: `complete_check`, `record_check_result`
  - Assert: neither `complete_check` nor `record_check_result` was called

- [x] **`test_perform_check_skips_when_target_enabled`**
  - Setup: test DB has a row with `enabled=True`
  - Mock: `complete_check` → returns a fixed fake `check_data` dict
  - Mock: `record_check_result`
  - Assert: `complete_check` was called with `(target.url, target.id, target.timeout_seconds)`
  - Assert: `record_check_result` was called with the correct unpacked fields from the mocked `check_data` return value

### `record_check_result`

- [x] **``test_record_checker_persists``**
  - Setup: real test DB (per existing test setup)
  - Call: `rd=None` (also exercises graceful-degradation path)
  - Assert: a `CheckResult` row exists in the test DB with the correct fields

- [x] **`test_record_check_result_skips_when_cache_true`**
  - Mock: `rd` — a Mock object standing in for the Redis client
  - Call: `record_check_result(..., cache=True)`
  - Assert: `rd.set` was called

- [x] **`test_record_check_result_skips_redis_when_cache_false`**
  - Mock: `rd`
  - Call: `record_check_result(..., cache=False)`
  - Assert: `rd.set` was never called

- [x] **`test_record_check_result_not_valid_to_cache`**
  - Call: `record_check_result(..., rd=None, cache=True)`
  - Assert: raises `ValueError`
